# TODO

## Fix: Blank screen when navigating while Profile/Settings/History modal is open

### Steps
- [x] Update `onChangeView` handler in `client/src/App.jsx` to close `profileOpen` and `historyOpen` overlays when navigating between views.
- [x] Verify the app builds/runs without errors.
